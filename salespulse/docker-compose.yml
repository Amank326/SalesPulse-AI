version: '3.8'
services:
  web:
    build: .
    ports:
      - "8001:8000"
    volumes:
      - .:/app
    environment:
      - SECRET_KEY=change_me
      - GEMINI_API_KEY=
    command: uvicorn backend.main:app --host 0.0.0.0 --port 8000 --reload
